export enum RoutesPublic {
  HOME = 'HOME',
  ACTIONS = 'ACTIONS',
  FUNDS = 'FUNDS',
  PREDICTIONS = 'PREDICTIONS',
}

export enum RoutesPrivate {}

export enum StacksRoutes {
  PRIVATE = 'PRIVATE',
  PUBLIC = 'PUBLIC',
}
