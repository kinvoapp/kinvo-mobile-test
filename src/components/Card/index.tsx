import React from 'react';

import { Text } from 'react-native';

import { Wrapper } from './styles';

const Card: React.FC = () => {
  return (
    <Wrapper>
      <Text>Card</Text>
    </Wrapper>
  );
};

export default Card;
