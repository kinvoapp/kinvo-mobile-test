import styled from 'styled-components/native'
import ScreenContainer from '../ScreenContainer'

const ListScreenContainer = styled(ScreenContainer)`
	padding: 0;
`

export default ListScreenContainer
