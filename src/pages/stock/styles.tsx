import styled from 'styled-components/native';
import colors from '../../resources/colors';

export const Container = styled.ScrollView`
  background: ${colors.gray03};
  height: 100%;
`;

export const ContentCardStock = styled.View`
  margin: 0 20px 20px;
`;
