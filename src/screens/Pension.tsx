import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const Pension = () => {
  return (
    <SafeAreaView>
      <Text>Pension</Text>
    </SafeAreaView>
  );
};

export default Pension;
