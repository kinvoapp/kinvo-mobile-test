import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignContent: "flex-start"
  },
  backButtonStyle: {
    flex: 15,
    backgroundColor: "#FFFFFF",
    alignItems: "center",
    justifyContent: "center"
  },
  topTabsContainerStyle: {
    flex: 85
  }
});

export default styles;
