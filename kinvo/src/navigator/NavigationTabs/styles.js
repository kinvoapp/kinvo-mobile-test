import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    height: 65,
    alignItems: 'flex-end'
  },
  tabBarStyle: {
    height: 70,
    paddingBottom: 5
  }
});

export default styles;
