import {
  StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  headerLeft: {
    marginLeft: 20,
  },
  flatListContent: {
    paddingBottom: 20,
  }
})

export default styles