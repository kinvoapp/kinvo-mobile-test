import {
  StyleSheet
} from 'react-native'

import colors from '../../util/colors'

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  headerLeft: {
    marginLeft: 20,
  },
  flatListContent: {
    padding: 8,
  }
})

export default styles