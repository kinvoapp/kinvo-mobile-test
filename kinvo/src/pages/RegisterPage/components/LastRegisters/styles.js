import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
    marginBottom: 10,  
  },
lastRegistersText: {
    alignSelf: "center", color: "#627179", fontSize: 16 
}
});

export default styles;
