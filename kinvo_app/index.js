/**
 * @format
 */

import { AppRegistry } from 'react-native';
import TabBottom from './src/TabBottom';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => TabBottom);
