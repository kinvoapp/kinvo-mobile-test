import styled from "styled-components"
import { Screen } from "../../styles"
import colors from "../../constants/colors"

export const LoadingScreen = styled(Screen)`
    justify-content: center;
    align-items: center;
    background-color: ${colors.loading}
`